services:
  - type: web
    name: municollect-frontend
    env: node
    buildCommand: |
      npm ci
      npx turbo build --filter=@municollect/shared
      npx turbo build --filter=frontend
    startCommand: npm run start --filter=frontend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /